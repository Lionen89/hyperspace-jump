import React from 'react';

function Destination() {
  return (
    // <main className={`destination ${isAnimationStart ? 'hyperspace__animation' : ''}`}>
    <h1 className="hyperspace__title">Современные технологии</h1>
    // {stars.map((item) => (
    // <div
    // className={`hyperspace__star ${isAnimationStart ? 'hyperspace__star-animation' : ''}`}
    // key={item}></div>
    // ))}
    // </main>
  );
}

export default Destination;
